#include <stdio.h>
#include <stdlib.h>

void printSentence();

int main()
{   
    printSentence();

    return 0;
}

void printSentence() {
   puts("1. In C, lowercase letters are significant."); 
   puts("2. main is where program execution begins."); 
   puts("3. Opening and closing braces enclose program statements in a routine."); 
   puts("4. All program statements must be terminated by a semicolon."); 
}